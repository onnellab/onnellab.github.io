---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getProductIndexItems, type Locale, type ProductIndexItem } from '../lib/products';

type Props = {
  locale: Locale;
};

const { locale } = Astro.props;
const contentApps = getProductIndexItems(locale);
const contentAppTitles = new Set(contentApps.map((app) => app.title));
const privacyUpdatedAtByTitle: Record<string, string> = {
  Quivra: '2026-02-10',
  TagWeaver: '2026-02-14',
  VaultXT: '2026-03-11',
  Segra: '2026-06-12',
  ClipNest: '2026-06-13',
  Aligna: '2026-06-24',
  Melivra: '2026-06-24'
};
const privacyDate = (title: string) => privacyUpdatedAtByTitle[title] ?? '9999-12-31';
const comparePrivacyDate = (a: string, b: string) =>
  privacyDate(a).localeCompare(privacyDate(b)) || a.localeCompare(b);
const compareAppPrivacyDate = (a: ProductIndexItem, b: ProductIndexItem) =>
  comparePrivacyDate(a.title, b.title);
const privacyApps = contentApps.filter((app) => app.privacy).sort(compareAppPrivacyDate);
const releasedApps = ['Quivra', 'TagWeaver', 'VaultXT', 'Segra', 'ClipNest', 'Aligna'].sort(
  comparePrivacyDate
);
const releasedAppCards = releasedApps
  .map((name) => contentApps.find((app) => app.title === name))
  .filter((app): app is ProductIndexItem => app !== undefined);
const featuredApp = releasedAppCards.find((app) => app.title === 'TagWeaver') ?? releasedAppCards[0];
const listedAppCards = releasedAppCards.filter((app) => app.title !== featuredApp?.title);
const appCardStyle = (app: ProductIndexItem) =>
  `--accent-border: ${app.accent.border}; --accent-bg: ${app.accent.background}; --accent-text: ${app.accent.text};`;
const staticPrivacyApps = [
  { title: 'Quivra', href: 'https://onnelakin.github.io/quivra-privacy-policy/' },
  { title: 'TagWeaver', href: 'https://onnelakin.github.io/tagweaver-privacy-policy/' },
  { title: 'VaultXT', href: 'https://onnelakin.github.io/vaultxt-privacy-policy/' },
  { title: 'Segra', href: 'https://onnelakin.github.io/segra-privacy-policy/' },
  { title: 'ClipNest', href: 'https://onnelakin.github.io/clipnest-privacy-policy/' },
  { title: 'Melivra', href: 'https://onnelakin.github.io/melivra-privacy-policy/' }
]
  .filter((app) => !contentAppTitles.has(app.title))
  .sort((a, b) => comparePrivacyDate(a.title, b.title));

const text =
  locale === 'ko'
    ? {
        title: 'ONNELLAB - 조용하고 구조적인 디지털 도구',
        description:
          'ONNELLAB은 음악, 텍스트, 오디오, 파일, 생산성을 위한 조용하고 구조적인 앱을 만드는 독립 소프트웨어 스튜디오입니다.',
        intro:
          '음악, 텍스트, 오디오, 파일 작업을 위한 조용한 도구를 만듭니다.',
        focus:
          '가볍게 열리고, 한 가지 일을 정확히 끝내는 앱에 집중합니다.',
        released: '출시된 앱',
        featured: '대표 앱',
        allApps: '전체 앱 페이지',
        allAppsHref: '/apps/ko/',
        openApp: '열기',
        upcoming: '예정 프로젝트',
        upcomingItems: ['Papira', 'FloMo', 'Nunote', 'Textoria', 'Heara'],
        privacy: '개인정보처리방침',
        contact: '문의',
        inquiry: '문의:',
        soon: '출시 예정',
        switchLabel: 'English',
        switchHref: '/'
      }
    : {
        title: 'ONNELLAB - Calm, Structured Digital Tools',
        description:
          'ONNELLAB is an independent software studio creating calm, structured apps for music, text, audio, files, and productivity.',
        intro:
          'Calm tools for music, text, audio, and file work.',
        focus:
          'Lightweight, privacy-conscious apps designed to do one job well.',
        released: 'Released Apps',
        featured: 'Featured App',
        allApps: 'All app pages',
        allAppsHref: '/apps/',
        openApp: 'Open',
        upcoming: 'Upcoming Projects',
        upcomingItems: ['Papira', 'FloMo', 'Nunote', 'Textoria', 'Heara'],
        privacy: 'Privacy Policy',
        contact: 'Contact',
        inquiry: 'For inquiries:',
        soon: 'Coming Soon',
        switchLabel: '한국어',
        switchHref: '/ko/'
      };
---

<BaseLayout
  title={text.title}
  description={text.description}
  canonicalPath={locale === 'ko' ? '/ko/' : '/'}
  lang={locale}
  alternates={[
    { lang: 'en', path: '/' },
    { lang: 'ko', path: '/ko/' },
    { lang: 'x-default', path: '/' }
  ]}
>
  <main class="page-shell">
    <nav aria-label="Language">
      <a href={text.switchHref}>{text.switchLabel}</a>
    </nav>

    <h1>ONNELLAB</h1>

    <section class="intro-block" aria-label="ONNELLAB">
      <p>{text.intro}</p>
      <p>{text.focus}</p>
    </section>

    {featuredApp && (
      <section class="featured" aria-label={text.featured} style={appCardStyle(featuredApp)}>
        <div class="featured-icon">
          <img src={featuredApp.iconPath} alt={`${featuredApp.title} icon`} width="72" height="72" />
        </div>
        <div>
          <span>{text.featured}</span>
          <h2>{featuredApp.title}</h2>
          <p>{featuredApp.description}</p>
          <div class="platform-badges" aria-label="Platforms">
            {featuredApp.platforms.map((platform) => <span>{platform}</span>)}
          </div>
        </div>
        <a href={featuredApp.href}>{text.openApp}</a>
        {featuredApp.screenshotPath && (
          <div class="featured-preview">
            <img
              src={featuredApp.screenshotPath}
              alt={`${featuredApp.title} preview`}
              width="1242"
              height="2688"
              loading="lazy"
              decoding="async"
            />
          </div>
        )}
      </section>
    )}

    <h2>{text.released}</h2>

    <section class="tool-list" aria-label={text.released}>
      {listedAppCards.map((app) => (
        <article class="tool-row" style={appCardStyle(app)}>
          <img src={app.iconPath} alt="" width="38" height="38" />
          <div>
            <h3>{app.title}</h3>
            <p>{app.description}</p>
            <div class="app-meta">
              <span>{app.status}</span>
              <div class="platform-badges" aria-label="Platforms">
                {app.platforms.map((platform) => <span>{platform}</span>)}
              </div>
            </div>
          </div>
          <a href={app.href}>{text.openApp}</a>
        </article>
      ))}
      <article class="tool-row upcoming-card">
        <div>
          <h3>Melivra</h3>
          <p>{text.soon}</p>
          <span>{text.upcoming}</span>
        </div>
      </article>
    </section>

    <p><a href={text.allAppsHref}>{text.allApps}</a></p>

    <h2>{text.upcoming}</h2>

    <p class="inline-list">
      {text.upcomingItems.map((item, index) => (
        <>
          <span>{item}</span>
          {index < text.upcomingItems.length - 1 && <span aria-hidden="true">/</span>}
        </>
      ))}
    </p>

    <h2>{text.privacy}</h2>

    <ul>
      {privacyApps.map((app) => <li><a href={app.privacy}>{app.title}</a></li>)}
      {staticPrivacyApps.map((app) => <li><a href={app.href}>{app.title}</a></li>)}
    </ul>

    <h2>{text.contact}</h2>

    <p>
      {text.inquiry}
      {' '}
      <a href="mailto:onnellab.app@gmail.com">onnellab.app@gmail.com</a>
    </p>
  </main>
</BaseLayout>

<style>
  .page-shell {
    max-width: 820px;
    margin: 80px auto;
    padding: 0 24px;
    line-height: 1.75;
  }

  nav {
    text-align: right;
    font-size: 14px;
  }

  nav a {
    text-decoration: none;
  }

  h1 {
    font-weight: 600;
    margin-bottom: 8px;
    letter-spacing: 0;
  }

  h2 {
    margin-top: 48px;
    font-weight: 600;
    letter-spacing: 0;
  }

  p {
    margin: 12px 0;
  }

  .intro-block {
    max-width: 610px;
    border-left: 2px solid #d8d0c5;
    margin: 22px 0 0;
    padding-left: 18px;
    word-break: keep-all;
    overflow-wrap: break-word;
  }

  .intro-block p {
    max-width: 560px;
  }

  ul {
    padding-left: 20px;
  }

  .featured {
    display: grid;
    grid-template-columns: 84px 1fr auto 136px;
    align-items: center;
    gap: 22px;
    border: 1px solid var(--accent-border);
    border-radius: 8px;
    margin: 38px 0 14px;
    padding: 22px;
    background: linear-gradient(90deg, var(--accent-bg), #fffdf8 74%);
  }

  .featured-icon img {
    width: 84px;
    height: 84px;
    border-radius: 20px;
  }

  .featured h2 {
    margin: 4px 0 6px;
    font-size: 28px;
  }

  .featured > div > span {
    color: var(--accent-text);
    font-size: 13px;
    font-weight: 650;
  }

  .featured p {
    margin: 0;
    color: #4f4b43;
    font-size: 16px;
    line-height: 1.55;
  }

  .featured > a {
    border: 1px solid var(--accent-border);
    border-radius: 8px;
    padding: 10px 14px;
    background: #fffdf8;
    color: #37342f;
    text-decoration: none;
    font-weight: 620;
  }

  .featured-preview {
    justify-self: end;
    width: 116px;
    max-height: 180px;
    border: 1px solid var(--accent-border);
    border-radius: 8px;
    padding: 5px;
    background: #fffdf8;
    overflow: hidden;
  }

  .featured-preview img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: 5px;
  }

  .tool-list {
    display: grid;
    gap: 0;
    border-top: 1px solid #e0d8cb;
    border-bottom: 1px solid #e0d8cb;
    margin: 18px 0 12px;
  }

  .tool-row {
    display: grid;
    grid-template-columns: 38px 1fr auto;
    align-items: center;
    gap: 18px;
    border-bottom: 1px solid #e9e1d5;
    padding: 15px 0;
  }

  .tool-row > img {
    width: 38px;
    height: 38px;
    border-radius: 10px;
    box-shadow: 0 0 0 1px var(--accent-border, #ddd5c8);
  }

  h3 {
    margin: 0 0 4px;
    font-size: 18px;
    line-height: 1.25;
    letter-spacing: 0;
  }

  .tool-row p {
    margin: 0;
    color: #4f4b43;
    font-size: 15px;
    line-height: 1.5;
  }

  .app-meta {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 7px;
  }

  .app-meta > span {
    color: #827d72;
    font-size: 13px;
    line-height: 1.4;
  }

  .platform-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 5px;
  }

  .platform-badges span {
    border: 1px solid var(--accent-border, #ddd5c8);
    border-radius: 999px;
    padding: 2px 7px;
    background: var(--accent-bg, #faf8f5);
    color: var(--accent-text, #827d72);
    font-size: 12px;
    line-height: 1.4;
  }

  .tool-row a {
    border: 1px solid var(--accent-border, #d6cec0);
    border-radius: 8px;
    padding: 8px 11px;
    background: #faf8f5;
    text-decoration: none;
    font-weight: 620;
    line-height: 1.2;
  }

  .upcoming-card {
    grid-template-columns: 1fr;
    color: #5b574f;
    border-bottom: 0;
  }

  .inline-list {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    color: #737067;
  }

  small {
    color: #777;
  }

  @media (max-width: 680px) {
    .page-shell {
      margin: 48px auto;
      padding: 0 18px;
    }

    .featured {
      grid-template-columns: 72px 1fr;
    }

    .featured-icon img {
      width: 72px;
      height: 72px;
      border-radius: 18px;
    }

    .tool-row {
      grid-template-columns: 38px 1fr;
      gap: 10px;
    }

    .tool-row a,
    .featured > a {
      grid-column: 1 / -1;
      text-align: center;
    }

    .featured-preview {
      grid-column: 1 / -1;
      justify-self: start;
      width: min(180px, 52vw);
      max-height: 220px;
    }
  }
</style>
