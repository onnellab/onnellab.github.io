name: Verify nine-language site

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/i18n-smoke.yml'
      - 'src/**'
      - 'public/privacy/**'
      - 'tests/**'
      - 'scripts/check-localization-quality.mjs'
      - 'package.json'
      - 'package-lock.json'
      - 'playwright.config.ts'
  pull_request:
    paths:
      - '.github/workflows/i18n-smoke.yml'
      - 'src/**'
      - 'public/privacy/**'
      - 'tests/**'
      - 'scripts/check-localization-quality.mjs'
      - 'package.json'
      - 'package-lock.json'
      - 'playwright.config.ts'
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

concurrency:
  group: i18n-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout site
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check localization quality
        run: npm run check:i18n-quality

      - name: Build site
        run: npm run build

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run nine-language product and site regression tests
        run: >-
          npx playwright test
          tests/site-i18n.spec.ts
          tests/extended-i18n.spec.ts
          tests/papira-i18n.spec.ts
          tests/product-contract.spec.ts
          tests/accessibility-i18n.spec.ts
          tests/localization-copy.spec.ts
          tests/site-visual.spec.ts
          --project=desktop

      - name: Publish commit status
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          JOB_RESULT: ${{ job.status }}
          TARGET_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ "$JOB_RESULT" = "success" ]; then
            state=success
            description="Nine-language localization regression tests passed"
          else
            state=failure
            description="Nine-language localization regression tests failed"
          fi

          gh api \
            --method POST \
            "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -f state="$state" \
            -f context="onnellab/i18n-smoke" \
            -f description="$description" \
            -f target_url="$TARGET_URL"
